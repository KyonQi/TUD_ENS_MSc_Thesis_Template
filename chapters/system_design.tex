\chapter{System Design}
\label{chp:system_design}
% In this section, we will present the design of our framework that transforms a standard single-device CNN into a communication-efficient distributed execution tasks across a cluster of networked MCUs. We will first discuss the overall architecture of coordinator-worker model, followed by the communication protocols. Two core optimizations techniques, i.e., \emph{block-wise local execution} and \emph{cross-block data reuse}, are proposed to allieviate the communication overhead, and will be discussed in detail. \textcolor{red}{We analyze the conditions for our proposed optimizations.} 
%% and provide a cost model to evaluate the trade-offs between communication and computation. Finally, we will discuss the implementation details of our framework.

In this chapter, we present the design of our framework that transforms a standard single-device CNN into a set of communication-efficient distributed execution tasks across a cluster of networked MCUs. We first describe the overall coordinator-worker architecture and a two-stage execution pipeline. Then, we detail the offline preprocessing stage, which quantizes the CNN model and compiles it into an execution plan in JSON format (Section~\ref{sec:offline-preprocessing}), and the online inference stage, which carries out the plan over a lightweight application protocol in TCP/IP (Section~\ref{sec:online-inference}). On top of this runtime inference, we develop two core optimization techniques that alleviate the communication and memory overhead of the system: \emph{block-wise local execution} (Section~\ref{sec:block-wise-local-execution}) and \emph{cross-block data reuse} (Section~\ref{sec:cross-block-data-reuse}). Between these two optimizations, we introduce an \emph{overlap-ratio cost model} (Section~\ref{sec:overlap-ratio-cost-model}) that characterizes when block-wise local execution stops paying off, and for cross-block data reuse we analyze the conditions under which it preserves correctness and improves performance (Section~\ref{sec:cross-conditions}). 

\section{System Architecture}
\label{sec:system-architecture}

\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth,height=0.3\textheight,keepaspectratio]{figs/fig_overview.pdf}
    \caption{Overview of our system architecture, comprising an offline preprocessing stage that compiles the model into an execution plan and per-worker manifests, and an online stage that executes the plan on a coordinator-worker cluster.}
    % \vspace{-4mm}
    \label{fig:system-architecture}
\end{figure}

A typical MCU offers only hundreds of KBs of SRAM (e.g., less than 512~KB) during runtime inference. While the model weights can be stored in Flash memory, the intermediate activations must reside in SRAM at runtime. For CNNs built from IRBs, the largest tensor at runtime is the output of the expanding convolution layer: each block first expands the channel dimension by an expansion ratio $e$ (e.g., $e=6$ in MobileNetV2), applies a depthwise convolution, and then reduces the channel dimension back to the original size~\cite{mobilenetv2}. The output of the expanding convolution layer is therefore $e$ times larger than the input, which can easily exceed the SRAM capacity of a single MCU. This results in a significant memory bottleneck that prevents the execution of large CNNs on resource-constrained MCUs. 

To address this, we implement an end-to-end pipeline for distributed inference across multiple networked MCUs, as shown in Figure~\ref{fig:system-architecture}, comprising two stages:
\begin{itemize}
    \item \textbf{Offline preprocessing stage}: It performs an 8-bit quantization of the model, and extracts the metadata describing the model weights and structural information. The metadata is emitted as JSON files and serialized into the Flash memory of each MCU at compilation time. An \emph{execution plan} is generated that describes the inference strategy and the data flow between the MCUs.
    
    \item \textbf{Online inference stage}: It executes the model on the distributed system across multiple networked MCUs. A single \emph{coordinator} holds the model metadata and the execution plan, partitions the workload to the \emph{workers} at each step, and reassembles the workers' outputs to produce the final inference result. Each \emph{worker} stores its share of the weights and activations, and performs the assigned computation tasks. The coordinator and workers communicate through a lightweight protocol on top of TCP/IP.
\end{itemize}

% As discussed in Section~\ref{chp:system_design}, a typical MCU offers only hundreds of KBs of SRAM (e.g., less than 512~KB) during runtime inference. While the model weights could be stored in the Flash memory, the intermediate activations have to reside in the SRAM at runtime, resulting in a significant memory bottleneck. 

% To address the issue, we implement a end-to-end pipeline shown in Figure~\ref{fig:tbd} that incorporates:
% \begin{itemize}
%     \item \textbf{Offline preprocessing stage} that quantizes the model, and extracts the metadata for the model weights and intermediate activations. The metadata will be released as JSON files and serialized into the Flash memory of each MCU at compilation time. An \emph{execution plan} will be generated to describe inference strategy and the data flow between the MCUs. 
    
%     \item \textbf{Online inference stage} that executes the model in the distributed system across multiple networked MCUs. A single \emph{coordinator} holds the model metadata and the execution plan, partitions the workload to the \emph{workers} at each step, and reassables the workers' outputs to produce the final inference result. Each \emph{worker} stores its share of the weights and activations and performs the assigned computation tasks. The coordinator and workers communicate through a lightweight protocol under TCP/IP. 
% \end{itemize}

\section{Offline Preprocessing}
\label{sec:offline-preprocessing}

The offline preprocessing stage employs a trained model for efficient distributed execution on resource-constrained MCUs. It quantizes the model weights and activations to 8-bit integers, extracts the per-layer metadata required at runtime, and compiles a precomputed communication-aware execution plan. The online inference therefore stays simple and makes no global decisions at runtime.


\subsection{Quantization}
Quantization is a key step in the offline preprocessing stage. By mapping floating-point weights and activations to low-bit integers, it reduces both the model size and the memory footprint and, crucially, replaces floating-point arithmetic with integer operations that MCUs can execute far more efficiently.
This makes quantization a prerequisite for on-device execution.

We employ 8-bit post-training quantization (PTQ)~\cite{quantization-ptq} with an asymmetric scheme. PTQ is chosen over quantization-aware training because it requires no access to the full training pipeline and only a small calibration set, which preserves our goal of reusing standard pretrained models without retraining~\cite{quantization-qat}. 
Under this scheme, activations are stored as \verb|uint8|, weights as \verb|int8|, and biases as \verb|int32|, with a separate scale per output channel for the weights. 
The asymmetric encoding lets the \verb|uint8| range fit the non-negative output of ReLU activations tightly, while per-channel weight scales accommodate the differing dynamic ranges across output channels that a single per-tensor scale would otherwise have to average over, at the cost of accuracy. 
For a convolutional layer, the integer accumulator and the requantized output are computed as follows:
\begin{align}
    \mathrm{acc} &= \sum(x_q - z_{\mathrm{in}})(w_q-z_w)+b_{\mathrm{int32}}, \label{eq:quantized-acc}\\
    y_q &= \mathrm{clip}(\mathrm{round}(\mathrm{acc} \cdot m + z_{\mathrm{out}}), 0, 255), \label{eq:quantized-y}\\
    m &= \frac{s_{\mathrm{in}}s_w}{s_{\mathrm{out}}}, \label{eq:quantized-multiplier}
\end{align}
where $(s,z)$ denotes the scale and zero-point of the input, weight, and output tensors, respectively. 
The multiplier $m$ is precomputed and stored per output channel, so that the runtime requantization in (\ref{eq:quantized-y}) reduces to a fixed-point multiply-and-shift with no floating-point division on the critical path. 
The quantization parameters are calibrated on a small number of representative samples, which is sufficient to estimate the activation ranges needed for the input and output scales.

For every layer of the CNN, the scales, zero-points, and precomputed
multipliers, together with the structural metadata (e.g., layer type,
input and output channels, kernel size, and residual links between layers) are
extracted and serialized into a single JSON file. This file is consumed directly
by the coordinator and is additionally emitted as C++ headers that are compiled
into each worker's Flash, so that both sides operate from an identical
description of the quantized model.

\subsection{Planner}
The planner compiles the quantized model graph into a communication-aware execution plan.
For each block of layers, it decides whether the block can be executed locally by the workers as a single unit or has to fall back to a layer-wise strategy (Section~\ref{sec:block-wise-local-execution}), and whether the block's output feature map can be safely reused by the next block (Section~\ref{sec:cross-block-data-reuse}).
These decisions are made offline and are strictly followed at runtime.

Lines~\ref{ln:offline-start}--\ref{ln:offline-end} of Algorithm~\ref{alg:plan-execute} summarise the offline planning stage.
The planner walks the blocks of $\mathcal{G}$ in order and creates one plan entry $p$ per block.
For each block, it first determines the \emph{execution mode} (line~\ref{ln:block-mode}), which decides whether the block can be executed by the workers as a single locally-computed unit.
It then evaluates whether \emph{cross-block reuse} can be enabled (line~\ref{ln:reuse-enable}), which the system can prevent repeated activations from being transferred across the network.
The completed entry is appended to $\mathcal{P}$, and the resulting plan is what the runtime consumes.

% maybe add more contents to the planner

\begin{algorithm}[t]
\small
\caption{The communication-aware execution planning and inference in our proposed framework}
\label{alg:plan-execute}
\KwIn{Model graph $\mathcal{G}$; workers $\mathcal{W}$; input $X$.}
\KwOut{Final prediction $Y$.}

\textbf{Offline planning:}\\
$\mathcal{P}\gets\emptyset$ \label{ln:offline-start}\\
\For{each block $B$ in $\mathcal{G}$}{
    $p\gets\textsc{CreatePlanEntry}(B)$\\
    \eIf{$B$ can be executed locally by workers}{
        $p.\textit{mode}\gets\textsc{BlockWiseLocalExecution}$\label{ln:block-mode}\\
    }{
        $p.\textit{mode}\gets\textsc{LayerWiseFallback}$\\
    }

    \eIf{the output of $B$ can be safely reused by the next block}{
        $p.\textit{reuse}\gets\textsc{EnableCrossBlockReuse}$\label{ln:reuse-enable}\\
    }{
        $p.\textit{reuse}\gets\textsc{DisableCrossBlockReuse}$\\
    }

    $\mathcal{P}\gets\mathcal{P}\cup\{p\}$ \label{ln:offline-end}\\
}

\textbf{Online inference:}\\
$T\gets\textsc{Quantize}(X)$ \label{ln:online-start}\\
\For{each plan entry $p$ in $\mathcal{P}$}{
    \For{each worker $w$ in $\mathcal{W}$}{
        $m_w\gets\textsc{BuildTask}(p,T,w)$\label{ln:build-task}\\
        $\textsc{SendTask}(w,m_w)$\\
    }

    \For{each worker $w$ in $\mathcal{W}$}{
        $r_w\gets\textsc{ReceiveResult}(w)$\label{ln:receive-result}\\
    }

    $T\gets\textsc{CombineResults}(\{r_w\}_{w\in\mathcal{W}})$\label{ln:combine-results}\\
}

$Y\gets\textsc{DecodePrediction}(T)$ \label{ln:online-end}\\
\Return{$Y$}
\end{algorithm}

\section{Online Inference}
\label{sec:online-inference}
The coordinator--worker architecture is designed to execute the distributed inference tasks efficiently.
As illustrated in Figure~\ref{fig:system-architecture}, the coordinator is responsible for task scheduling and data aggregation.
Following the execution plan produced offline, it partitions the input activations, wraps and assigns the computation tasks to the workers, and gathers their results to form the output activations.
Each worker executes its assigned task by loading the model weights from Flash, selecting the suitable computation kernel, and performing the computation on the input activations.
The online part of Algorithm~\ref{alg:plan-execute} (lines~\ref{ln:online-start}--\ref{ln:online-end}) details this runtime workflow: for each plan entry, the coordinator builds a per-worker task (line~\ref{ln:build-task}), dispatches it, collects the partial results (line~\ref{ln:receive-result}), and combines them before advancing to the next entry (line~\ref{ln:combine-results}).



\subsection{Coordinator Design}
\label{sec:coordinator-design}

\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth]{figs/coordinator-components.pdf}
    \caption{Component architecture of the coordinator.
    The inference engine drives execution and holds all inference state, reaching the worker manager and the task distributor through a unified interface.
    The task distributor obtains its block grouping strategy from a pluggable strategy mapper provided by the offline plan, and all components run on a shared asynchronous I/O event loop.}
    % \vspace{-4mm}
    \label{fig:coordinator-components}
\end{figure}

The coordinator is an event-driven server, built on an asynchronous I/O event loop to handle multiple concurrent connections from workers.
As shown in Figure~\ref{fig:coordinator-components}, it is organized into a set of decoupled components:
\begin{itemize}
    \item \textbf{Inference engine}: drives the execution based on the preprocessed execution plan and holds all inference state.
    \item \textbf{Worker manager}: maintains the connection state of each worker, including the network connections and the message framing.
    \item \textbf{Strategy mapper}: works as a pluggable component that determines how consecutive layers are grouped into blocks offline.
    \item \textbf{Task distributor}: splits the workload and communicates the resulting tasks to the workers.
\end{itemize}

Figure~\ref{fig:coordinator-workflow} illustrates the coordinator's workflow as the messages exchanged among its components and the workers.
A worker first joins through a registration handshake: it sends a \emph{Register} message to the worker manager, which replies with an \emph{ACK} once the worker has been assigned an ID.
Driven by the engine, the task distributor then consults the \emph{strategy mapper} for the block grouping decided offline and dispatches the per-worker tasks to the worker manager.
The worker manager forwards each task to its worker as a \emph{Task} message and, once the worker finishes, returns the corresponding \emph{Result} message.
The distributor collects these results and issues an \emph{Update Inference} to the engine, which updates the inference state and advances to the next block.
This loop repeats until every block in the plan has been executed. During the inference, the engine also handles error conditions such as worker disconnections or timeouts, retrying or reassigning tasks as needed.

The design principle of the coordinator is to abstract the independent components and provide a unified interface for the inference engine to drive the execution.
The engine is agnostic to \emph{how} tasks reach workers, which is the distributor's responsibility, and to \emph{how} layers are grouped into blocks, which is the strategy mapper's responsibility.
This decoupling makes the coordinator flexible: a single engine can support different models, different numbers of workers, and different strategies for block grouping and task distribution without modification.

\paragraph{Inference Engine.}
The inference engine is the stateful core of the coordinator and drives the execution of the inference plan.
It maintains the state of the inference process, including the current block or layer being executed, the input and output activations, and the intermediate results.

\paragraph{Worker Manager.}
The worker manager abstracts the network transport from the rest of the system and owns the TCP connection to each worker.
When a worker connects, it first completes a short registration handshake defined at the application layer: the worker announces itself and reports its clock frequency, and the coordinator replies with a unique worker ID.
Each connection is then tracked by a state machine that maintains the connection state and handles message framing.
% Nagle's algorithm is disabled on every connection so that small task descriptors are sent without delay.
Idle workers are held in a queue from which the inference engine draws when dispatching the next round of tasks.
When the manager detects a disconnection from the workers, it immediately shuts down and resets the status of all existing connections, keeping the transport robust and fault-tolerant.
% Disconnections and reconnections are handled gracefully, keeping the transport robust and fault-tolerant.

\paragraph{Strategy Mapper.}
The strategy mapper is a pluggable component that decides how consecutive layers of the model are grouped into blocks offline. The resulting block structure is the unit at which the inference engine advances and the task distributor splits work, so the mapper effectively defines the boundaries at which activations are exchanged between the coordinator and the workers.
Because it is consulted only offline and exposed through a fixed interface, 
users can easily extend the system for different grouping strategies, and enable them without changing the engine or the distributor.


\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth]{figs/coordinator-workflow.pdf}
    \caption{Coordinator workflow. Messages exchanged among the inference engine, task distributor, worker manager, and a worker during one round of execution.}
    % \vspace{-4mm}
    \label{fig:coordinator-workflow}
\end{figure}

\paragraph{Task Distribution.} 
The task distributor carries out the splitting of workload and the communication with workers. Compared with the \emph{inference engine}, it is stateless with respect to the inference process. Consider a layer (or block) that consumes a feature map of shape $(C,H,W)$, where $C$ is the number of channels, $H$ is the height, and $W$ is the width, and whose convolution has kernel size $k$, stride $s$, and padding $p$. The output height of this layer is
\begin{equation}
    H_{\mathrm{out}} = \left\lfloor \frac{H + 2p - k}{s} \right\rfloor + 1,
\end{equation}
and the output width $W_{\mathrm{out}}$ is computed similarly. 

The distributor splits the $H_{\mathrm{out}}$ output rows into $N$ chunks, where $N$ is the number of workers. Worker $i$ is therefore produces the output rows $[o_s^i, o_e^i)$, where the start and end rows are computed as:
\begin{align}
    o_s^i &= i\left\lceil \frac{ H_{\mathrm{out}}}{N} \right\rceil, \\
    o_e^i &= \min\left((i+1)\left\lceil \frac{ H_{\mathrm{out}}}{N} \right\rceil, H_{\mathrm{out}}\right).
\end{align}

Due to the nature of convolution, the input rows required by worker $i$ have to extend beyond the output row band. For each worker, the range of input rows $[i_s^i, i_e^i)$ is computed as:
\begin{align}
    i_s^i &= \max\left(o_s^i s - p, 0\right), \\
    i_e^i &= \min\left(o_e^i s - p + k, H\right).
\end{align}  

Therefore, two adjacent workers $i$ and $i+1$ have overlapping input rows bounded by $k-s$ rows. It is the redundant computation overhead that will be further discussed in Section~\ref{sec:overlap-ratio-cost-model}. 

For the fully connected layer, the distributor simply splits the the layer by output class with $N$ chunks and assigns worker $i$ to compute the output classes $[o_s^i, o_e^i)$, where
\begin{align}
    o_s^i &= i\left\lceil \frac{ C_{\mathrm{out}}}{N} \right\rceil, \\
    o_e^i &= \min\left((i+1)\left\lceil \frac{ C_{\mathrm{out}}}{N} \right\rceil, C_{\mathrm{out}}\right).
\end{align}




\subsection{Worker Design}
\label{sec:worker-design}
\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth]{figs/worker_state_machine.pdf}
    \caption{Worker state machine, comprising an initialization phase (\emph{Disconnected} through \emph{Idle}) and an event-driven loop that processes one task at a time.}
    % \vspace{-4mm}
    \label{fig:worker-state-machine}
\end{figure}

A worker is a lightweight executor driven entirely by the tasks it receives from the coordinator without a scheduler of its own.
As shown in Figure~\ref{fig:worker-state-machine}, there are two phases during its life cycle.

During the \emph{initialization phase}, the worker moves from \emph{Disconnected} to \emph{Connecting} as it establishes the TCP connection, then to \emph{Registering}, where it completes the registration handshake with the worker manager mentioned in Section~\ref{sec:coordinator-design} by announcing itself and receiving its worker ID in the application layer.
It then settles into \emph{Idle}, ready to serve tasks.

During the inference, the worker runs an \emph{event-driven loop}. On receiving a task it enters \emph{Receiving Task}, reconstructs the input activations from the received data, and loads the corresponding weights from Flash.
After loading, it transitions to \emph{Computing}, where it selects the appropriate computation kernel based on the layer type and the available memory, and performs the computation.
Finally, in \emph{Sending Result} it returns the output activations to the coordinator and falls back to \emph{Idle} to await the next task.
When encountered a malformed or out-of-order task, the worker takes the \emph{Task Invalid} transition, which is reported to the coordinator rather than executed.





\subsection{Protocol Design}
\label{sec:protocol-design}

\begin{table}[t]
\centering
\caption{Message types of the protocol.}
\label{tab:message-type}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l l p{6.2cm}}
% \hline\hline
\hline
\textbf{Message} & \textbf{Direction} & \textbf{Purpose} \\
\hline
\textsc{Register} & worker $\rightarrow$ coordinator &
announce the device and report its clock frequency \\
\textsc{Register Ack} & coordinator $\rightarrow$ worker &
accept the worker and assign its logical identifier to the physical device \\
\textsc{Task} & coordinator $\rightarrow$ worker &
carry one plan entry as a descriptor followed by its tensor payload \\
\textsc{Result} & worker $\rightarrow$ coordinator &
return the on-device timing followed by the output rows \\
\textsc{Error} & worker $\rightarrow$ coordinator &
report a typed failure, such as a stale reuse cache or an exhausted buffer \\
\textsc{Shutdown} & coordinator $\rightarrow$ worker &
end the session \\
\hline
% \hline\hline
\end{tabular}
\end{table}


To support the coordinator--worker architecture, we design a custom, lightweight protocol on top of TCP/IP.
Following the coordinator and worker designs, the protocol must satisfy four requirements:
\begin{itemize}
    \item \textbf{Dynamic admission.} It must admit workers dynamically and bind each physical device to the logical worker ID assigned by the coordinator, so that the coordinator can manage the worker set and assign tasks at runtime.
    \item \textbf{Self-contained tasks.} It must carry a task descriptor complete enough for a worker to execute a layer or block without prior knowledge of the model, such as the layer or block index range, the input and output dimensions, and the convolution parameters.
    \item \textbf{Reliable framing.} It must frame variable-size payloads reliably over the byte stream, so that messages of arbitrary size can be transferred.
    \item \textbf{Uniform support.} It must support both naive distributed
    inference and our proposed optimizations through a single and unified format,
    informing worker at runtime whether optimization should be enabled.
\end{itemize}

% These requirements lead to a small custom binary protocol on top of TCP.
% TCP provides the reliable and ordered delivery that multi-kilobyte tensor transfers need, which a datagram transport such as UDP would fail and have to rebuild by hand.
% A custom binary encoding, rather than a general serialisation format, keeps the parsing cost on the worker near zero.
% Each message is a fixed-layout record that the MCU reads directly into a structure, without any parser code, dynamic allocation, or per-field interpretation.
% The generality of a self-describing format brings little cost, since the descriptor spans only tens of bytes while the payloads frames span hundreds of times than the header, up to kilobytes.
% TCP also helps in managing the flow of data. The protocol needs no sequence numbers or flow control of its own, allieviating the burden on the system.
% Each worker holds at most one computation task, and the plan already fixes a total order over the steps, so a stop-and-wait rhythm of one task and one result per round is all the coordination the system requires.

These requirements lead to a small custom binary protocol over TCP, with three design decisions.

First, we build on TCP rather than a datagram transport.
Multi-kilobyte tensor transfers need reliable, ordered delivery. Over UDP, the protocol would have to rebuild those guarantees by hand.
TCP also contains the built-in flow control, so the protocol does not need sequence numbers or windowing of its own.

Second, we use a custom binary encoding rather than a general serialization format, which keeps the worker's parsing cost near zero.
Each message is a fixed-layout record that the MCU reads directly into a struct, without any parser, dynamic allocation, or per-field interpretation.
% A self-describing format would cost little here, since the descriptor spans only tens of bytes, while the payload it accompanies is hundreds of times larger, up to several kilobytes.

Third, we adopt a strictly ordered transport strategy. Each worker holds at most
one task at a time, and the offline plan already fixes a total order over the
steps, so one task and one result per round is all the system
needs. There is no reordering or in-flight windowing layered on top of TCP.

\paragraph{Message type.}
As illustrated in Table~\ref{tab:message-type}, the protocol defines six message types grouped by inference phase.
In the initialization phase, a worker announces itself with a \emph{registration} message reporting its clock frequency, and the coordinator replies with an \emph{acknowledge} message carrying an assigned worker identifier.
This identifier binds the physical device to the logical index the plan refers to, so the plan never depends on the physical board order.
In the execution phase, the coordinator issues one \emph{task} message per worker for each plan entry and receives one \emph{result} message in return.
The remaining two types handle exceptions and shutdown. A worker raises a typed \emph{error} message when it cannot serve a task, for instance when its reuse cache is stale or an input exceeds its buffer. And the coordinator ends the session with a \emph{shutdown} message.
% The vocabulary contains five message types, grouped by inference phase in Figure~\ref{fig:coordinator-workflow}.
% In the initialization phase, a worker announces itself with a \emph{registration message} that reports its clock frequency, and the coordinator responds with an \emph{acknowledge message} containing an assigned worker identifier.
% The identifier binds the physical device to the logical index the plan refers to, so the plan never depends on which board occupies which port.
% In the execution phase the coordinator issues one \emph{task message} per worker for each plan entry, and receives one \emph{result message} in return, and this loop repeats until the network is exhausted.
% The remaining types handle exceptions and teardown.
% A worker raises a typed \emph{error message} when it cannot serve a task, for instance when its reuse cache is stale or an input exceeds its buffer, and the coordinator ends the session with a shutdown message.

\paragraph{Framing.}
Every message shares the same frame.
A sixteen-byte header carries a magic number, the message type, the worker identifier, and the length of the payload that follows.
The magic number lets a receiver validate and resynchronize the stream, and the length field lets it read exactly one message before interpreting the next.
For a task, the bulk tensor data follows the descriptor within the same round.
The descriptor states how many payload bytes arrive, and the reuse flags state how to interpret them: either as a full input patch, or as the top and bottom boundary rows that complement a cached slice.
Descriptor and data therefore reach the worker together, so each plan step costs exactly one round trip per worker, without separate control and data channels.

\paragraph{Task descriptor.}

\begin{table}[t]
\centering
\caption{Field groups of the task descriptor. Each descriptor materialises one plan entry and is self-contained.}
\label{tab:sd-task}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l p{8.2cm}}
\hline
\textbf{Group} & \textbf{Content} \\
\hline
\\
Kernel Information & Input and output dimensions, kernel, stride, padding, groups,
per-worker depthwise padding 
\vspace{2mm} \\
\hline
\\
Block-wise execution & Layer type, first and last layer index \vspace{2mm}\\
\hline
\\
Cross-block reuse & Cache rebuild flag, boundary row counts, cached row range,
expected producer block, return-boundary flag \vspace{2mm} \\
\hline
\end{tabular}
\end{table}


The task descriptor is the materialization of one plan entry, and its fields fall into the three groups of Table~\ref{tab:sd-task}.
\begin{itemize}
    \item \emph{Kernel information} group carries the input and output dimensions, and the size of convolution parameters that enable the naive inference in the system;
    \item \emph{Block-wise execution} group names the layer type together with the first and last layer indices, so the same message expresses a single layer when the two indices coincide and a fused block when they span an IRB. Therefore, we can easily extend our optimization in Section~\ref{sec:block-wise-local-execution} for workers;
    \item \emph{Cross-block reuse} group carries the boundary geometry of Section~\ref{sec:cross-block-data-reuse}, informing workers whether to rebuild the input from the local cache, how many boundary rows are attached, which cached rows to use, which block must have produced the cache, and whether to return boundary rows only.
\end{itemize}




Four properties of this descriptor carry the design.
It is \emph{self-contained}: a worker needs nothing beyond the descriptor, its payload, and its Flash-resident weights to execute a step.
It is \emph{uniform}: every optimization field defaults to zero, so a plain layer-wise task simply leaves them unset, and the baseline and every combination of the two mechanisms run over the identical protocol---later comparisons between execution modes therefore carry no protocol differences.
It is \emph{self-checking}: the expected producer index acts as a freshness token for the worker's cache; a worker whose cache does not match the token reports an error instead of computing on stale rows, and the coordinator falls back to a full transfer, realizing the invalidation rule of Section~\ref{sec:cross-conditions} at runtime.
And it is \emph{cheap}: the descriptor spans tens of bytes against tensor payloads of kilobytes.


\paragraph{Result message.}
The result message closes each round.
It reports the worker's on-device timing and the size of the output that follows, and the output itself is either the worker's full output slice or only its boundary rows, as the return flag of the task requested.
Carrying the timing inside the protocol separates the time a worker spent computing from the time its bytes spent on the wire, which lets the coordinator attribute per-step latency to computation or communication and underpins the measurements reported later in this thesis.
The byte-level encoding of these messages, and the discipline that keeps the coordinator-side and worker-side definitions in lockstep, are implementation concerns and are described in the implementation chapter.





% \textcolor{red}{Still need tables for message structure.}
% The coordinator and workers communicate over a custom, lightweight protocol built on top of TCP/IP. \textcolor{red}{As illustrated in Table~\ref{tab:tbd}, every message begins with a fixed-byte header that indicates the message type and payload length, followed by the typed payload.} Figure~\ref{fig:coordinator-workflow} shows the basic message flow: (i) a worker registers and reports its status; (ii) the coordinator acknowledges with an assigned identifier; (iii) the coordinator then issues \textsc{TASK} messages and receives \textsc{RESULT} messages until the inference is complete.

% Table~\ref{tab:tbd} summarizes the information carried in \textsc{TASK} message to support a worker in executing a layer or block. \textcolor{red}{the layer or block index range, the input and output dimensions, the convolution parameters, the per-worker depthwise height padding $p^i_{\mathrm{top}}, p^i_{\mathrm{bot}}$ from (\ref{eq:sd-inpad}), and the cross-block reuse fields---whether to reconstruct the input from the local cache, how many boundary rows are sent, which cached rows to use, and whether to return only boundary rows. The \textsc{Result} message returns the compute timing and the output size, followed by the output bytes. The concrete byte-level encoding and the matching layouts on the Python and C++ sides are deferred to the implementation chapter.}

% The task descriptor is the materialisation of one task entry, and its fields are demonstrated in Table~\ref{tab:sd-task}.
% The execution range names the layer type together with the first and last layer indices, so the same message expresses a single layer when the two indices coincide and a fused block when they span an IRB.
% The geometry group carries the input and output dimensions, the convolution parameters, and the per-worker depthwise padding of (\ref{eq:sd-inpad}).
% The reuse group carries the boundary geometry of Section~\ref{sec:cross-block}, namely whether to rebuild the input from the local cache, how many boundary rows are attached, which cached rows to use, which block must have produced the cache, and whether to return boundary rows only.

% Four properties of this descriptor carry the design.
% It is self-contained, since a worker needs nothing beyond the descriptor, its payload, and its Flash-resident weights to execute a step.
% It is uniform, since every optimisation field defaults to zero and a plain layer-wise task simply leaves them unset, so the baseline and every combination of the two mechanisms run over the identical protocol and later comparisons between execution modes carry no protocol differences.
% It is self-checking, since the expected producer index acts as a freshness token for the worker's cache.
% A worker whose cache does not match this token reports an error instead of computing on stale rows, and the coordinator falls back to a full transfer, which realises the invalidation rule of Section~\ref{sec:cross-block-conditions} at run time.
% And it is cheap, since the descriptor spans tens of bytes against tensor payloads of kilobytes.


 
% To optimize performance and resource utilization, we have implemented a block-wise local execution strategy. This approach allows us to process data in smaller chunks, reducing memory overhead and improving execution speed. Each block is processed independently, allowing for parallel execution and better load balancing across the system. This design choice also enhances fault tolerance, as failures in one block do not affect the processing of others.
\section{Block-wise Local Execution}
\label{sec:block-wise-local-execution}


\begin{figure}[t]
  \centering
  \begin{subfigure}{0.45\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figs/fig_compare_a_sub.pdf}
    % \vspace{-2mm}
    \caption{Layer-wise communication.}
    \label{fig:sub_a}
  \end{subfigure}
  \hspace{2mm}
  \begin{subfigure}{0.45\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figs/fig_compare_b_sub.pdf}
    % \vspace{-2mm}
    \caption{Block-wise local execution.}
    \label{fig:sub_b}
  \end{subfigure}
  % \vspace{-3mm}
\caption{Layer-wise comm. vs. block-wise local execution.}
  \label{fig:block-wise-local-execution}
  % \vspace{-4mm}
\end{figure}


A natural strategy to orchestrate the distributed execution of a CNN is to split the model in a layer-wise manner, where the coordinator splits a layer by rows, dispatches the tasks to workers, collects the partial outputs, reassembles the full feature map, and re-scatters it for the next layer. 
This strategy is simple and straightforward, widely adopted by most distributed inference runtimes~[\textcolor{red}{refs}]. 

However, it incurs a significant communication overhead for IRB-based CNNs as discussed in Section~\ref{sec:IRB}. 
After the expanding layer, the number of channels is expanded by a factor of $e$, resulting in a large feature map that must be communicated between the coordinator and workers. 
This large feature map traverses the network twice and must be held in full at the coordinator, which is a significant bottleneck for resource-constrained MCUs. 


Our Block-wise local execution moves the communication boundary from the layer to the block.
As shown in Figure~\ref{fig:block-wise-local-execution}, instead of exchanging data at every layer, each worker executes an entire IRB locally on its assigned spatial input slice.
The coordinator sends only the block input and receives only the projected output, which is typically $e$ times smaller than the expanded activation.
The expanded activation is materialized in the worker's local buffer and never leaves the device.
For the first IRB of MobileNetV2, this removes up to 1176~KB of intermediate traffic per inference.


\paragraph{Coordinator Memory.}
Beyond the traffic reduction, block-wise local execution also shrinks the coordinator's memory footprint.
Under the layer-wise strategy, the coordinator must hold the entire expanded feature map, which typically dominates its SRAM usage.
Consider one IRB with input shape $(C_{\mathrm{in}}, H_{\mathrm{in}}, W_{\mathrm{in}})$, expansion ratio $e$, and output shape $(C_{\mathrm{out}}, H_{\mathrm{out}}, W_{\mathrm{out}})$.
Its peak coordinator memory under the layer-wise strategy is approximately
\begin{equation}
\mathcal{M}^{\mathrm{layer}}_{\mathrm{coord}}(B) \approx e\, C_{\mathrm{in}} H_{\mathrm{in}} W_{\mathrm{in}} + C_{\mathrm{out}} H_{\mathrm{out}} W_{\mathrm{out}}.
\label{eq:sd-mem-layer}
\end{equation}

Our block-wise local execution removes the former expanded term completely. The coordinator only needs to hold the block input and output activation. The peak memory footprint of the coordinator under block-wise strategy is therefore:
\begin{equation}
\mathcal{M}^{\mathrm{block}}_{\mathrm{coord}}(B) \approx C_{\mathrm{in}} H_{\mathrm{in}} W_{\mathrm{in}} + C_{\mathrm{out}} H_{\mathrm{out}} W_{\mathrm{out}}.
\end{equation}

Since the output activation is typically much smaller than the expanded activation, the block-wise local execution strategy significantly reduces the memory footprint of the coordinator to:
\begin{equation}
\Delta = \frac{\mathcal{M}^{\mathrm{block}}_{\mathrm{coord}}(B)}{\mathcal{M}^{\mathrm{layer}}_{\mathrm{coord}}(B)} \approx \frac{1}{e}.
\end{equation}  

Take the first IRB of MobileNetV2 as an instance, a layer-wise coordinator must hold the block input and the expanded activation, requiring $196$ KB $+$ $1176$ KB = $1372$ KB. With block-wise local execution, the coordinator only handles the block input and the projected output, reducing the footprint to $196$ KB $+$ $73.5$ KB $=$ $269.5$ KB, which is approximately $\frac{1}{e}$ ($e=6$) of the layer-wise strategy.

\paragraph{Overlap-ratio Cost Model.}
\label{sec:overlap-ratio-cost-model}

\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth]{figs/kernel-overlap.pdf}
    \caption{Overlapping input rows for two adjacent workers. Disjoint outputs require input slices that overlap by $K-s$ rows whenever $K>1$.}
    % \vspace{-4mm}
    \label{fig:kernel-overlap}
\end{figure}

Block-wise local execution trades communication for computation, as each worker must compute the overlapping input rows required by its neighbors. Figure~\ref{fig:kernel-overlap} illustrates the overlapping input rows for two adjacent workers $i$ and $i+1$. The number of overlapping rows between two adjacent workers is determined by the kernel size $k$ and stride $s$ of the convolution, and is given by:
\begin{equation}
    R_{\mathrm{overlap}} = k - s.
    \label{eq:overlap}
\end{equation}

Under layer-wise strategy, there is no redundant computation for the overlapping rows, since output rows are partitioned and assigned to a single worker only once. 
However, under block-wise local execution, although the output rows of projection layer are still partitioned and assigned to workers only once, each worker must compute the overlapped slice during the local execution of the depthwise convolution. We capture this overlapped redundancy with the \emph{overlap ratio}:
\begin{equation}
    \rho = \frac{1}{H}\sum_{i=0}^{N-1}(y_e^i - y_s^i),
    \label{eq:overlap-ratio}
\end{equation}
where $H$ is the height of the input feature map, $N$ is the number of workers, and $[y_s^i, y_e^i)$ is the input row range assigned to worker $i$. 
The overlap ratio $\rho$ represents the fraction of the input feature map that is redundantly computed by multiple workers. 
A higher overlap ratio indicates more redundant computation, which can offset the communication savings achieved by block-wise local execution. 
The ideal $\rho=1$ indicates there is no overlap among the workers, whereas $\rho\to N$ is the worst case where all workers has to contain the entire input feature map.

Combined~(\ref{eq:overlap}) with~(\ref{eq:overlap-ratio}), we can express the overlap ratio as a function of the kernel size $k$, stride $s$, and the number of workers $N$:
\begin{equation}
    \rho(k, s, N) \approx 1 + \frac{(N-1)(k - s)}{H}.
    \label{eq:overlap-ratio-func}
\end{equation}

(\ref{eq:overlap-ratio-func}) demonstrates three key factors that affect the overlap ratio: 
\begin{itemize}
    \item Number of workers $N$: $\rho$ grows with the worer count, indicating that finer partitioning of the input feature map leads to more overlap and redundant computation.
    \item Kernel size $k$ and stride $s$: A larger kernel size or a smaller stride increases the number of overlapping rows due to the nature of the convolution, leading to a higher overlap ratio.
    \item Height of the input feature map $H$: A taller input feature map reduces the overlap ratio, as the overlapping rows become a smaller fraction of the total input.
\end{itemize}

Therefore, the block-wise local execution effect is strongly position-dependent. For an early high-resolution block where $H=112$ with a $3\times3$ depthwise kernel on $N=4$ workers, the overlap ratio is $\rho\approx 1.05$, indicating a nearly free gain. However, for a deep low-resolution block where $H=7$ with a larger $5\times5$ kernel on $N=4$ workers, the overlap ratio is $\rho\approx 2.7$, indicating a significant amount of redundant computation that can easily offset the communication savings.

\paragraph{Adaptive selection.}
This position dependence motivates us a per-block decision on whether to apply block-wise local execution or fall back to layer-wise strategy. (\ref{eq:model-selection}) formalizes our adaptive selection of execution mode for each block $B$ based on the overlap ratio $\rho(B)$ and a manual tunable threshold $\tau$. If the overlap ratio is below the threshold, we apply block-wise local execution; otherwise, we revert to layer-wise strategy. 
\begin{equation}
    mode(B) = \begin{cases}
        \text{block-wise local execution}, & \text{if } \rho(B) \leq \tau, \\
        \text{layer-wise strategy}, & \text{otherwise},
    \end{cases}
    \label{eq:model-selection}
\end{equation}

The threshold can be determined empirically based on the specific model architecture and hardware characteristics. We set $\tau=2$ in our the system. A faster network prefers a smaller $\tau$, since communication is cheap and redundant expansion dominates the cost, while a bandwidth-constrained network prefers a larger $\tau$. 




% (i) the kernel size $k$, (ii) the stride $s$, and (iii) the number of workers $N$. A larger kernel size or a smaller stride increases the number of overlapping rows, leading to a higher overlap ratio. Similarly, increasing the number of workers also increases the overlap ratio, as more workers share the same input feature map.

\section{Cross-block Data Reuse}
\label{sec:cross-block-data-reuse}
% To further optimize performance, we have implemented a cross-block data reuse mechanism. This allows data generated in one block to be efficiently utilized in subsequent blocks, reducing redundant computations and improving overall system efficiency.

\begin{figure}[t]
    \center
    \includegraphics[width=\linewidth]{figs/fig_Overlapped_Compute.pdf}
    \caption{Cross-block data reuse.}
    % \vspace{-4mm}
    \label{fig:cross-block-data-reuse}
\end{figure}



Block-wise local execution still leaves redundant communication between consecutive blocks. After a worker finishes block $b$, most of its output is exactly the data it needs to begin block $b+1$. A naive strategy returns this data back to the coordinator and immediately sends it back to the same worker again, wasting the limited bandwidth and inflating coordinator memory. To address this, we propose to reuse cross-block data. As illustrated in Figure~\ref{fig:cross-block-data-reuse}, it keeps reusable data on the worker and exchanges only the small boundary region the next block requires.

% \textcolor{red}{Rephrase needed.} Rather than discarding its output once finished with block $b$, a worker caches the rows it produced for block $b$ in its local buffer. The interior rows of the output need not cross the network, since they are exactly the rows required by the next block $b+1$. The only data that must still move between two consecutive blocks is the thin boundary region that a worker's neighbor requires but does not produce itself.

Concretely, consider worker $i$ needs input rows $[a_i, b_i)$ for block $b+1$, while it has already cached rows $[s_i, e_i)$ from block $b$. The cached rows cover the intersection $[\max(a_i, s_i),\min(b_i, e_i))$, so the only rows worker $i$ lacks are the top and bottom boundary rows: 
\begin{equation}
    \beta^i_{\mathrm{top}} = \max(0, s_i - a_i),
    \qquad
    \beta^i_{\mathrm{bot}} = \max(0, b_i - e_i),
    \label{eq:boundary}
\end{equation}
which is produced by the neighbouring workers $i-1$ and $i+1$. 

Therefore, our cross-block data reuse significantly reduces communication overhead in both directions during inference.
On the downlink, the coordinator forwards only the boundary rows each worker lacks instead of a full input patch. 
On the uplink, a worker returns only the boundary rows its neighbours will need instead of its full output. 
The interior rows are never transmitted over the network, which significantly reduces the communication overhead between consecutive blocks. 
The coordinator now holds only boundaries and acts as a lightweight scheduler rather than a full data aggregator, lowering its peak memory footprint. 

\paragraph{Conditions to Enable Cross-block Data Reuse.}
\label{sec:cross-conditions}

\begin{table}
    \centering
    \caption{Conditions to enable cross-block data reuse.}
    \begin{tabular}{m{3.5cm} m{7.0cm}}
        \hline
        \textbf{Reuse direction} & \multicolumn{1}{c}{\textbf{Enabling condition}} \\
        % \hline \hline
        \hline
        Coordinator $\rightarrow$ worker & Previous block has no residual connection. \\
        \hline
        Worker $\rightarrow$ coordinator &
            \begin{itemize}[leftmargin=*, topsep=0pt, itemsep=1pt, nosep]
                \item Current block has no residual connection;
                \item Next block neither caches nor consumes a residual;
                \item Next block is spatial rather than fully connected.
            \end{itemize} \\
        \hline
    \end{tabular}

\label{tab:reuse-conditions}
\end{table}

% \begin{table}[t]
% \centering
% \caption{Conditions to enable cross-block data reuse.}
% \label{tab:reuse-conditions}
% \footnotesize
% \setlength{\tabcolsep}{10pt}
% \begin{tabular}{c c}
% \hline
% \textbf{Reuse Direction} & \textbf{Enabling Conditions} \\
% \hline
% Coordinator $\rightarrow$ worker & Previous block has no residual connection. \\
% Worker $\rightarrow$ coordinator & (i) Current block has no residual connection; (ii) Next block neither caches nor consumes a residual; (iii) Next block is spatial rather than fully connected.  \\
% \hline
% \end{tabular}
% \end{table}


Cross-block data reuse is employed only when it does not compromise the accuracy of the inference stage. The enabling conditions are evaluated in the offline preprocessing stage and encoded in the task metadata at runtime, so the workers simply follow the execution plan. There are two situations that make a worker's local data invalid to reuse.

The first is the residual addition. When a block consumes a residual, the coordinator dequantizes the cached residual and the current block output, adds them together, and requantizes the sum in place as
\begin{equation}
    y = \mathrm{clip}(\mathrm{round}(\frac{(c-z_c)s_c+(r-z_r)s_r}{s_t} + z_t), 0, 255),
\end{equation}
where $(s,s_c,z_c)$ denotes the current block output with its scale and zero-point, and $(r,s_r,z_r)$ denotes the cached residual. This computation changes the cached data in the coordinator, while the worker's cache still holds the pre-residual output. Therefore, the worker's cache is invalid and cannot be reused for the next block. The following block must fall back to a full transfer that replaces the worker's cache with the coordinator's updated data.

The second situation is global average polling together with the fully connected layer that consumes the pooled output. These operations squeeze the spatial dimensions of the feature map into a single value per channel, leaving no rows to reuse.

As a result, we summarize the conditions that enable cross-block data reuse in Table~\ref{tab:reuse-conditions}. In practice, whether cross-block data reuse is enabled is determined by the architecture of the CNN. It is most valid on the early, high-resolution blocks where the boundary savings are also largest. 


% \begin{table}[t]
%     \centering
%     \begin{tabular}{| l | c | r |}
%     \hline
%     left aligned & centred & right aligned \\
%     \hline \hline
%     12 & 34 & 56 \\
%     \hline
%     \end{tabular}
%     \caption{Complete sentence describing the tabular data. Caption should summarize the conclusions of the result.}
%     \label{tab:table_1}
% \end{table}
